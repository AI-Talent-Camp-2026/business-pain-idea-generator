# Система отслеживания покупок

## Обзор

Система отслеживания записывает каждое нажатие на кнопку "💳 Купить (подробнее)" в детальной карточке идеи. Данные сохраняются в базе данных и доступны через защищенную admin панель.

## Что отслеживается

При каждом нажатии на кнопку "Купить" сохраняется:
- **ID идеи** - какая идея заинтересовала пользователя
- **ID run** - из какого прогона генерации
- **Дата и время** - когда произошло нажатие
- **IP адрес** - для базовой аналитики уникальности
- **User Agent** - информация о браузере/устройстве

## Структура базы данных

### Таблица `purchases`

```sql
CREATE TABLE purchases (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    idea_id INTEGER NOT NULL,
    run_id VARCHAR,
    created_at DATETIME NOT NULL,
    user_ip VARCHAR(50),
    user_agent VARCHAR(500),
    FOREIGN KEY (idea_id) REFERENCES ideas(id),
    FOREIGN KEY (run_id) REFERENCES runs(id)
)
```

## API Endpoints

### 1. Записать покупку (публичный)

```http
POST /api/purchases
Content-Type: application/json

{
  "idea_id": 123
}
```

**Ответ:**
```json
{
  "success": true,
  "message": "Спасибо, запрос отправлен.",
  "purchase_id": 456
}
```

### 2. Получить статистику (защищенный)

```http
GET /api/admin/purchases
X-API-Key: your-admin-api-key
```

**Ответ:**
```json
{
  "total_purchases": 150,
  "recent_purchases": [...],
  "top_ideas": [
    {
      "idea_id": 5,
      "title": "Название идеи",
      "purchase_count": 25
    }
  ]
}
```

## Доступ к Admin панели

### Локально (Development)

1. Откройте `http://localhost:8080/admin.html`
2. Введите API ключ: `change-me-in-production` (по умолчанию)
3. Нажмите "Загрузить"

### Production

1. **Обязательно** установите сложный API ключ в переменной окружения:
   ```bash
   export ADMIN_API_KEY="111"
   ```

2. Откройте `https://yourdomain.com/admin.html`

3. Введите ваш API ключ

## Настройка для Production

### 1. Установка API ключа

Создайте/обновите файл `.env`:

```bash
# Backend .env
ADMIN_API_KEY=your-very-secure-random-key-12345
```

### 2. Генерация безопасного ключа

```bash
# Linux/Mac
openssl rand -hex 32

# Python
python -c "import secrets; print(secrets.token_urlsafe(32))"
```

### 3. Защита admin страницы (опционально)

Рекомендуется настроить дополнительную защиту на уровне веб-сервера:

**Nginx:**
```nginx
location /admin.html {
    auth_basic "Admin Area";
    auth_basic_user_file /etc/nginx/.htpasswd;
}
```

**Apache:**
```apache
<Files "admin.html">
    AuthType Basic
    AuthName "Admin Area"
    AuthUserFile /path/to/.htpasswd
    Require valid-user
</Files>
```

## Статистика в Admin панели

### Метрики

1. **Всего покупок** - общее количество нажатий
2. **Уникальных идей** - количество идей с хотя бы одной покупкой
3. **Последних 24ч** - активность за последние сутки

### Топ идей

Показывает 10 самых популярных идей по количеству покупок:
- Рейтинг
- Название идеи
- Количество покупок

### История покупок

Последние 50 покупок с информацией:
- ID покупки
- Название идеи
- Дата и время
- IP адрес пользователя

## Миграция базы данных

При развертывании на новом сервере:

```bash
cd backend
python migrate_add_purchases.py
```

## Безопасность

### Что реализовано:
- API key авторизация для admin endpoints
- Запись IP только для базовой аналитики
- Отсутствие персональных данных пользователей

### Рекомендации:
1. Используйте HTTPS в production
2. Регулярно меняйте ADMIN_API_KEY
3. Ограничьте доступ к admin панели по IP (firewall/nginx)
4. Настройте rate limiting для API
5. Регулярно делайте backup базы данных

## GDPR/Compliance

Собираемые данные:
- IP адреса (анонимизированы, используются только для статистики)
- Никаких email, имен или других ПД
- Пользователь не идентифицируется персонально

Для полного соответствия GDPR рекомендуется:
1. Добавить Cookie consent banner
2. Добавить Privacy Policy
3. Реализовать механизм удаления данных по запросу

## Мониторинг

### Логи

Все операции логируются в `logs/app.log`:
```bash
tail -f logs/app.log | grep Purchase
```

### Прямой доступ к БД

```bash
sqlite3 pain_to_idea.db

# Количество покупок
SELECT COUNT(*) FROM purchases;

# Топ идей
SELECT idea_id, COUNT(*) as cnt
FROM purchases
GROUP BY idea_id
ORDER BY cnt DESC
LIMIT 10;

# Покупки за сегодня
SELECT COUNT(*) FROM purchases
WHERE date(created_at) = date('now');
```

## Troubleshooting

### Ошибка "401 Unauthorized"
- Проверьте правильность API ключа
- Убедитесь, что ADMIN_API_KEY установлен в .env

### Покупки не записываются
- Проверьте логи backend: `tail -f logs/app.log`
- Убедитесь, что миграция применена
- Проверьте CORS настройки

### Admin панель не загружается
- Проверьте, что backend запущен
- Проверьте URL в `API_BASE_URL` (app.js)
- Откройте консоль браузера (F12) для ошибок

## Дальнейшие улучшения

Можно добавить:
- [ ] Email уведомления при новых покупках
- [ ] Webhook для интеграции с CRM
- [ ] Экспорт статистики в CSV/Excel
- [ ] Графики и charts в admin панели
- [ ] A/B тестирование разных вариантов кнопки
- [ ] Интеграция с Google Analytics
- [ ] Реальная платежная система

## Контакты

При вопросах или проблемах создайте issue в репозитории проекта.
